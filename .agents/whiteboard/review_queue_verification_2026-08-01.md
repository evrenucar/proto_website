# Review queue, machine-checked, 2026-08-01

Agent opus5-30. Every `[A]` card in `todo.md` (70 of them) re-checked by running the thing it
names: the suite, the file, the endpoint, the count, the string. Nothing here is reasoning about
whether a check would pass.

**4 contradicted · 51 verified by machine · 15 need your eyes.**

Nothing in the repo was changed. The mirror used for the break tests is
`.tmp/scratch/opus5-30/mirror/`, a copy; `JavaScript/braindump.js` and `CSS/braindump.css` on disk
were never touched.

---

## Contradicted: 4

### 1. Arrow-key panning. The suite still cannot fail on either of the two things the card says were fixed. `!p3`

**The card says** a reviewer proved the first version of the test blind twice over: it passed with
the speed cap removed, and passed with the "nothing selected" gate removed. It reads as a fixed
problem.

**The machine says** the suite on disk today is blind to both, still.

| break, applied in the mirror | what a person would see | `arrow-key-pan.test.mjs` |
| --- | --- | --- |
| speed cap removed | board runs away: **7613 px/s** at 3.6s and still climbing, against **2247 px/s** capped | **green** |
| "nothing selected" gate removed | a selected node no longer stops the pan: camera moved **798px** with a node selected, against 0px intact | **green** |

Both breaks were measured live before being called breaks, so this is the suite failing to see a
real regression, not a patch that failed to land.

Why it stays green: the plateau check compares two 400ms windows only 400ms apart, and linear
unbounded acceleration over that gap is a ratio of 1.31 against a 1.5 threshold. The gate is
untested because both selection phases use a YouTube node and a note being typed into, and each
returns earlier in the handler than the pan branch, so neither phase ever reaches the gate.

```
cd .tmp/scratch/opus5-30/mirror
node patchfile.mjs JavaScript/braindump.js 'const t = Math.min(heldMs / ARROW_PAN_ACCEL_MS, 1);' 'const t = heldMs / ARROW_PAN_ACCEL_MS;'
node tests/board/arrow-key-pan.test.mjs   # exit 0
```

The feature itself works. It is the proof that is worthless, on the exact card that spends a
paragraph congratulating itself for catching this.

### 2. `cosmoboard-landing.html` "re-synced by hand". The page is a build output.

The card says the page is hand-maintained, that it was updated by hand, that this is the third
time the chore has come up, and that "generating the page would end it". A different `[A]` card
lower in the file says it has been generated by the build since 2026-07-30, and the machine agrees
with the second one.

```
ls src/templates/cosmoboard-landing.template.html   # exists
grep -c cosmoboard-landing scripts/build-site.mjs   # 4
```

The hashes the card quotes are also gone. Landing and onboarding now both serve
`braindump.css?v=b610b97af346`, `braindump.js?v=b95cf35f2678` and
`data-board-source-version="03788d85b5aa"`, byte-identical to each other. The card is stale and
its recommendation is already done.

### 3. The VNC/iframe research note still carries the factual error a later card corrects.

`.agents/research/vnc_and_iframe_embedding_2026-07-30.md:40` still reads "Electron/Tauri webviews
are not subject to X-Frame-Options". The 2026-08-01 embedding-options card says that is wrong,
that both engines enforce it, and that the correction flips the recommendation from Tauri to
Electron. The old note is the one linked from the roadmap and it has not been corrected or
pointed at the new one, which that card's own "How to check" line asks for.

```
sed -n '39,41p' .agents/research/vnc_and_iframe_embedding_2026-07-30.md
```

### 4. "Current score 42/47 (89%)" on the machine-readability card.

The scorer prints 47/47. Superseded by the backfill card above it, but the number on this card is
now false and it is the one a reader hits first.

```
node tests/benchmarks/machine-readability.mjs   # machine-readability score: 47/47 (100%)
```

---

## Verified by machine: 51

Every suite below was run one at a time on this machine today. All green.

| # | Card | Settled by |
| --- | --- | --- |
| 1 | Pen/eraser size drag past the screen edge | probe: pointer lock taken on long press, size 4 to 1 at the screen edge, lock released on mouseup |
| 2 | Canvas tool, nesting, rename `!p1` | `node tests/board/canvas-tool-and-rename.test.mjs` |
| 3 | Markdown overflow gradient halved | `CSS/braindump.css` `.bd-markdown-overflow-gradient` `height: 22px` |
| 4 | Tracker scroll position + card ids `!p2` | `node tests/features/tracker-scroll-and-ids.test.mjs` |
| 5 | VNC node built `!p2` | `node tests/board/vnc-node.test.mjs` (7/7); 69 files under `JavaScript/vendor/novnc/` |
| 6 | Markdown download bundles images | `node tests/export/markdown-base64-embedding.test.mjs` (zip carries the image bytes + rewritten refs) |
| 7 | Embed overflow fixed | `.bd-embed-iframe` is `flex:1; height:auto`; link/app/board-preview shells all `overflow:hidden` |
| 8 | Dev board findable and full `!p2` | 23 nodes, 9 types incl. base/app/entity; tracker footer links dev and test board |
| 9 | Perf budget + CI `!p2` | `node tests/board/perf-budget.test.mjs`; `.github/workflows/board-tests.yml` on `pull_request` |
| 10 | Iframe-refusing fallback cards `!p3` | `node tests/preview/preview-frame-check-endpoint.test.mjs` |
| 11 | Benchmark backfill 47/47 `!p3` | `node tests/benchmarks/machine-readability.mjs` |
| 12 | CLI over board data `!p2` | `node scripts/cosmo.mjs boards` (6 boards); `node tests/cli/cli-over-board-data.test.mjs` |
| 13 | Terminal deliberately not built `!p3` | `node tests/board/computer-window-node.test.mjs` (8/8: Terminal entry, no Connect button) |
| 14 | Touch parity `!p2` | `node tests/board/touch-gesture-parity.test.mjs` (6/6) |
| 15 | Shortcuts panel behind `?` `!p2` | `node tests/board/shortcuts-panel.test.mjs`: "36 rows", and 5 advertised gestures actually performed |
| 16 | Test audit `!p3` | `.agents/whiteboard/test_audit_2026-08-01.md` present; its Ctrl+S finding is fixed (see #26) |
| 17 | Lazy embeds `!p1` | probe: near node 1 iframe, far node 0 iframes and a placeholder. **No suite covers this** |
| 18 | Selection as state `!p2` | `node tests/board/esc-deselect.test.mjs` (5/5) |
| 19 | GitHub repo sync `!p1` | probe: save issues `GET .../contents/<path>?ref=main` then `PUT`, auth header sent, commit message `cosmoboard: update <path>`. **No suite covers this** |
| 20 | Tracker compact mode `!p2` | `#compact-toggle` button, `body.compact` rules, persisted in localStorage |
| 21 | Tracker heading wrap `!p2` | `overflow-wrap: anywhere` on `.card-title` and `.card-note` |
| 22 | Tracker Backlog column + Done collapsed `!p2` | lane `{ key: ".", title: "Backlog", mark: "[.]" }`, Done `collapsed: true`, server marker regex `[ xA~.]` |
| 23 | Iframe-refusing answer note `!p3` | file present (but see contradiction 3) |
| 24 | Performance analysis note `!p3` | `.agents/whiteboard/performance_analysis_2026-07-30.md` |
| 25 | Landing page positioning `!p2` | `sitemap.xml` first loc is the landing page and it holds the only `<priority>1.0</priority>`; both pages `index,follow` |
| 26 | All four public PDFs down `!p1` | only `content/boards/dev/bayblend-fr3010-pc-abs.pdf` remains; nodes `nfw8czu22uq` and `fjam96so57` gone; 54 to 52 nodes confirmed at commit `6b7ca46` |
| 27 | Ctrl+S saves the board `!p1` | source is the three-arm chord as described, **and the repair is proved**: reverting Ctrl+S to `saveLocalFile()` in the mirror turns `board-save-export-runtime` red on the bounded regex and `board-save-reload-e2e` red at 4s |
| 28 | Canvas endpoint restored | `curl /api/list-canvas?board=test-board` returns 200 |
| 29 | Drawing colour follows the accent | `node tests/board/theme-reset-and-pen-colour.test.mjs`, red when `getPenStrokeColor()` is hard-coded |
| 30 | Hidden toolbar 8px off the bottom, small hover target | `node tests/board/toolbar-dock-position.test.mjs`, red for both a 50px offset and a shell that takes the pointer |
| 31 | Settings panel scrolls, board stays put | `node tests/board/overlay-wheel-scroll.test.mjs` (3/3) |
| 32 | Previews resize both ways, no spill | `node tests/board/node-size-and-overflow.test.mjs` |
| 33 | Pinch survives a finger on a note | `node tests/board/mobile-pinch-zoom.test.mjs` (5/5), and the file **is** git-tracked (an open card says it is not) |
| 34 | Recommendation panel fits at 1024px | `node tests/features/toolbar-panel-viewport-fit-e2e.test.mjs` (30 measurements, 10 widths) |
| 35 | Underscore emphasis (and its duplicate card) `!p5` | `node tests/board/markdown-caret-offset-mapping.test.mjs` (72/72); regex present at both call sites |
| 36 | "Test failures" lane resolves | 0 CRLF in `todo.md`, all 7 lane headings resolve; `node tests/preview/preview-todo-update-endpoint.test.mjs` |
| 37 | Embedding answer note | `.agents/research/vnc_and_iframe_embedding_2026-07-30.md` present |
| 38 | Markdown authoring e2e un-parked | `node --test tests/features/markdown-authoring-e2e.test.mjs`, tree clean after |
| 39 | Suites no longer flake in parallel | 4 build suites launched simultaneously in the mirror, all green (the 5th needs `.agents/`, absent from the mirror) |
| 40 | One-click entry into notes | `node tests/board/markdown-drag-and-title.test.mjs` |
| 41 | RDP: computer-window protocol switch `!p3` | `node tests/board/computer-window-node.test.mjs` (8/8) |
| 42 | Agents age out of the tracker feed | covered by `tracker-scroll-and-ids` ("agent aging") |
| 43 | Return to default colours `!p2` | `node tests/board/theme-reset-and-pen-colour.test.mjs` |
| 44 | Mobile testing harness `!p2` | `node tests/board/mobile-diagnostics-harness.test.mjs` (6/6); `.agents/whiteboard/mobile_testing_guide.md`, 245 lines, iOS covered explicitly |
| 45 | Perf benchmark, re-creatable `!p1` | same seed twice gives a byte-identical board (`sha256 170993d7…` both runs); generator and driver both refuse to write under `content/` |
| 46 | Eraser erases segments `!p1` | `node tests/board/pen-and-eraser-tools.test.mjs`, red when the split is replaced by a whole-stroke delete |
| 47 | Pen revamp: alt-scroll, long-press, size circle `!p2` | same suite, plus the pointer-lock probe in #1 |
| 48 | Pin drag + Windows-style snap `!p1` | `node tests/board/pin-move-and-snap.test.mjs` (14/14), red when `pinSnapZoneAt` always returns null |
| 49 | Light mode + theme group `!p2` | `node tests/board/theme-and-light-mode.test.mjs` (7/7) |
| 50 | Embed header shows the real address, double-click copies | probe: header reads the full video URL, clipboard matches, toast "Link copied". **No suite covers this** |
| 51 | Shift-constrained drag; page lock; toolbar auto-hide; pin to viewport; YouTube timeline; Alt menu suppression; owner cards sort first; landing generated by build | `shift-constrained-drag` 9/9 · `toolbar-autohide-and-lock` (prints 44x16 / 76x48) · `pin-to-viewport` 12/12 · `youtube-player-controls` · `alt-key-menu-suppression` · tracker sorts `owner` before priority · `cosmoboard-build` + template + matching hashes |

### Adversarial spot-checks on the "verified" group

Nine deliberate breaks in the mirror, on the seven highest-stakes cards whose whole proof is one
suite. **Six caught, one not.**

| card | break | suite |
| --- | --- | --- |
| Ctrl+S `!p1` | revert to `saveLocalFile()` | **caught** (both suites) |
| Canvas tool `!p1` | disable canvasId healing after a rename | **caught** |
| Canvas tool `!p1` | stop storing the child's id on the node | **caught** |
| Eraser `!p1` | delete the whole stroke instead of splitting | **caught** |
| Pin snap `!p1` | snap zones always null | **caught** |
| Pen accent `!p4` | hard-code the pen colour | **caught** |
| Toolbar dock | tab floats 50px off the bottom | **caught** |
| Toolbar dock | whole collapsed shell takes the pointer | **caught** |
| Arrow-key pan `!p3` | speed cap removed, and gate removed | **missed both** (contradiction 1) |

Three cards carry no suite at all and were settled by hand probes instead: lazy embeds, GitHub
sync, and the embed-header address. They pass, but nothing in CI will notice when they stop.

---

## Needs your eyes: 15

No command can settle these. One line each on what to look at.

1. **VNC `!p2`** Point the node at a real KasmVNC or websockify host and confirm it resumes on reload. The mock server proves the protocol; only you have a real machine.
2. **Dev board `!p2`** 23 nodes are there. Judge whether the showcase row is the right showcase.
3. **Terminal not built `!p3`** Accept the deferral, or overrule it and take the four security requirements as a card.
4. **GitHub sync `!p1`** Needs your private repo and a fine-grained token. The API call is provably correct; whether a commit lands is yours.
5. **Iframe-refusing recommendation `!p3`** Read the four options and say whether ruling out the rewriting proxy is right.
6. **Performance plan `!p3`** Read the ranking and say whether incremental saves or staged mount comes next.
7. **Proposed roadmap `!p2`** Awaiting approval by design. It is the last section of `holistic_planning.md`.
8. **"Some websites refuse to be embedded"** Read the answer; the first two rungs are built.
9. **RDP `!p3`** The verdict is "you are half right, not yet". Agree or overrule.
10. **Settings menu `!p1`** Every measurable thing passes (containment at 3 widths, one label edge, one control edge, AA contrast both modes). Whether it now looks clean is the whole card. Also open: the panel is still about 2.8 screens tall, and the Help group probably belongs in the shortcuts panel now.
11. **Filesystem integration** The card has no proof text at all, but `.agents/research/filesystem_integration_2026-08-01.md` exists. Read it.
12. **Firefox/app/distro `!p2`** Wants an explicit yes or no on Electron before anyone scaffolds a shell. See contradiction 3 for its unfinished follow-up.
13. **Realtime collaboration `!p3`** Read the note, then judge the GitHub-sync card's framing.
14. **Runtime vs framework** Accept "keep the custom runtime primary" or not.
15. **Mobile testing `!p2`** The guide and the harness exist. Doing it needs your phone and your iPad.

Several cards in the verified list also end with an explicit question of yours: whether preview
text should scale with the box, whether pin edges should resize rather than move, whether pinned
items should stop growing, whether existing strokes should repaint when the accent changes, and
whether the embed header should sweep-select instead of drag. Those are answered on the card, not
here.

---

## One thing this pass could not do

A card whose proof is a suite is only as good as the suite, and I broke seven of them. The other
44 verified rows rest on a suite I read but did not mutate. The arrow-key-pan result is the
argument for doing more of this: that card was the single most confident one in the queue about
its own test quality, and it was the one that was wrong.

Working notes and every command: `.tmp/scratch/opus5-30/notes.md`.
