# Tasks

## Current request

- [x] Replace the token-based Notion sync with a token-free public shared-page flow.
- [x] Let the site pull title, content, media, and last-updated time from public Notion page URLs.
- [x] Keep card placement and labels controllable per item through a local manifest.
- [x] Update the GitHub Action so it no longer depends on Notion secrets.
- [x] Document the token-free setup and add an example manifest.

## Next

- [ ] Create `src/notion-public-pages.json` with the real public Notion page URLs.
- [ ] Add more project and open-quest pages through the shared-page manifest.
- [ ] Refine media rendering if future Notion pages rely heavily on complex embeds.
