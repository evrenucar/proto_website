# Markdown node

Markdown nodes render real `.md` files inline on the board.

- Headings, lists, and links work as you would expect
- The file lives on disk at the path the node points to
- Editing here saves back to that same file

Open the [Cosmoboard direction note](/content/boards/cosmoboard/direction.md) for a longer example.
